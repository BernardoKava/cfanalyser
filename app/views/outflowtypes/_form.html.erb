<%= form_with(model: outflowtype, local: true) do |form| %>
  <% if outflowtype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(outflowtype.errors.count, "error") %> prohibited this outflowtype from being saved:</h2>

      <ul>
      <% outflowtype.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :system_item %>
    <%= form.check_box :system_item %>
  </div>

  <div class="field">
    <%= form.label :active %>
    <%= form.check_box :active %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
